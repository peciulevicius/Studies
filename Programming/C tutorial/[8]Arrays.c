#include <stdio.h>
#include <stdlib.h>


///Arrays is used to define a container where we store a lot of random values
int main()
{
    /*
    int luckyNumbers[] = {4, 8, 15, 16, 23, 42};
    printf("%d", luckyNumbers[0]);   ///we're accessing number 4 the first number in the array
    */

    /*
    int luckyNumbers[] = {4, 8, 15, 16, 23, 42};
    luckyNumbers[1] = 200; ///this replaces the second number in the array with 200
    printf("%d", luckyNumbers[1]);  ///prints the second number that is changed
    */

    /*
    int luckyNumbers[10]; ///it puts 10 values
    luckyNumbers[1] = 80; ///giving values for the array
    luckyNumbers[0] = 90; ///giving values for the array

    printf("%d", luckyNumbers[1]);
    */

    return 0;
}
