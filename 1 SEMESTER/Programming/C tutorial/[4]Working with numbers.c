#include <stdio.h>
#include <stdlib.h>

int main()
{
    ///*This whole code will not work*///
    ///*Because all of the codes are just examples*///

    1.
    printf("%f", 5 / 4.1); ///if we devide it with %d it will give us only 1


    2.
    int num = 6;
    printf("%d", num);


    3.
    printf("%f", pow(2, 3)); ///This is 2^3 (2x2x2=8)
    /// %d will not work because because we need a decimal number


    4.
    printf("%f", sqrt(36));


    5.
    printf("%f", ceil(36.356));
    ///ceil stands for ceiling (it will round the number
    ///we can do *floor* instead of ceil. It will round it down

    return 0;
}
